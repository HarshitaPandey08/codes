#include<stdio.h>
#include<stdlib.h>
#include<string.h>


int main()
{
    int n,i,f=0;
    //scanf("%d",&n);
    char s[100],t[100];
    scanf("%s",s);
    scanf("%s",t);
    if(strlen(s)==strlen(t))
    {
        for(i=0;i<strlen(s);i++)
        {
            if(s[i]!=t[i])
            {
                f++;
                break;
            }
        }
    }
    else
    {
        f++;
    }
    if(f==0)
    {
        printf("YES");
    }
    else
    {
        printf("NO");
    }
}

    
    
