#include<stdio.h>
#include<math.h>
int fact(int f)
{
    int i=1;
    while(f>0)
    {
        i=i*f;
        f--;
    }
    return i;
    
    
}
int main()
{
    int a,b,c,r,n,s;
    scanf("%d %d",&n,&r);
    a=fact(n);
    b=fact(n-r);
    c=fact(r);
    s=a/(b*c);
    printf("%d",s);
}
