#include<stdio.h>
#include<string.h>

int main()
{
    char s[100],t[100],c;
    scanf("%s",s);
    scanf("%s",t);
    int i,f=0;
    for(i=0;i<strlen(s);i++)
    {
        if(s[i]!=t[i])
        {
            c=s[i];
            f++;
            break;
        }
    }
    if(f!=0)
    {
        printf("%c",c);
    }
    else
    {
        printf("NA");
        
    }
    
}
